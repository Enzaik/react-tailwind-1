// export const getClasses = (all, sm, md, lg, xl) => {
//   if (sm) {
//     const newSm = sm.split(' ').reduce((acc, current) => {
//       acc + `sm:${current}`;
//     });
//   }
//   return newSm;
// };

// getClasses('py-5', 'py-4', null, null)
